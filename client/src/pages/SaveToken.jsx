import { useEffect } from 'react'
import { useLocation } from 'react router
import jwt from 'jsonwebtoken'

const SaveToken = ({ setUser }) => {
   const query =
  return <div>this is the saveToken page</div>;
};

export default SaveToken;
